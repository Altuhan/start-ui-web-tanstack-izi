{
  "$schema": "https://openapi.vercel.sh/vercel.json",
  "buildCommand": "pnpm run build",
  "installCommand": "pnpm install",
  "framework": null,
  "env": {
    "IZIMAG_FRONTEND_ONLY": "true",
    "SKIP_ENV_VALIDATION": "true"
  }
}
